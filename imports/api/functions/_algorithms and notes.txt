

overlap replies
dont overlap replies



if fsmodel topmost platform above canvas
  expand up
    window.scrollTo(0, 0);  //not 0,0

recursive thing
  thing(platform)
    if (leftIsLower(platform))
      return thing(left) + width(platform)
    else
      return width(platform)

  **** if left edge, 1000?